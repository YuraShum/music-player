export type SongType = {
    _id: string,
    title: string,
    artist: string,
    mp3: string,
    cover: string
}

export type PlaylistType = {
    _id: string,
    name: string,
    description: string,
    songs: string[]
}